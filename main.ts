function VolumeOfCuboid (L: string, B: string, H: string) {
    let height = 0
    let breadth = 0
    let length = 0
    volume = length * (breadth * height)
    player.say("Volume of Cuboid is")
    player.say("volume")
}
let volume = 0
volume = 0
VolumeOfCuboid("5", "6", "10")
